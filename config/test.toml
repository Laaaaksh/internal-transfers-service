# Internal Transfers Service - Test Configuration
# Overrides for test environment (unit tests, integration tests)

[app]
env = "test"

[database]
host = "localhost"
port = 5432
user = "postgres"
password = "postgres"
name = "transfers_test"
ssl_mode = "disable"
max_connections = 5
min_connections = 1

[logging]
level = "error"
format = "json"

[metrics]
enabled = false

[idempotency]
ttl = "1h"
